================================================================
  QualtricsToLime — Security Audit (Bandit Static Analysis)
================================================================

  Tool:    Bandit 1.9.4 (Python security linter)
  Date:    2026-06-26
  Target:  app.py, qsf_to_lss.py, qsf_inspector.py, convert.py

================================================================
  IMPORT ANALYSIS
================================================================

  All imports across the entire codebase (4 files, 1458 lines):

  STANDARD LIBRARY ONLY:
    argparse, collections, csv, datetime, json, os, pathlib,
    re, sys, typing, xml.dom.minidom, xml.etree.ElementTree

  NETWORKING IMPORTS:     None
  SUBPROCESS IMPORTS:     None
  FILESYSTEM IMPORTS:     pathlib, os (input/output folders only)
  THIRD-PARTY IMPORTS:    None

================================================================
  SCAN RESULTS
================================================================

  Total lines of code:    1458
  High severity issues:   0
  Medium severity issues: 2 (false positives — see below)
  Low severity issues:    3 (false positives — see below)

================================================================
  FINDINGS (all false positives)
================================================================

  Finding 1-5: XML parsing of "untrusted" data
  Severity: Low / Medium
  Files: app.py:16, app.py:137, qsf_to_lss.py:25-26, qsf_to_lss.py:640

  Bandit flags any use of xml.etree and xml.dom.minidom as
  potentially vulnerable to XML bomb attacks (billion laughs).

  WHY THIS IS A FALSE POSITIVE:
  - app.py parses LSS files that the application ITSELF just
    generated seconds earlier — not external untrusted input
  - qsf_to_lss.py uses xml.etree to BUILD XML (Element,
    SubElement) and minidom only to pretty-print its OWN output
  - No XML from external/untrusted sources is ever parsed
  - The input format is JSON (.qsf), not XML

================================================================
  ADDITIONAL MANUAL VERIFICATION
================================================================

  The following checks were performed manually:

  [OK] No network imports (socket, urllib, http, requests,
       aiohttp, httpx, ftplib, smtplib, ssl)
  [OK] No subprocess or shell execution (subprocess, os.system,
       os.popen, exec, eval)
  [OK] No file access outside input/output folders
  [OK] No environment variable reads (beyond sys.executable
       for PyInstaller frozen path detection)
  [OK] No temporary file creation outside app directory
  [OK] No logging to external services
  [OK] No data serialization to hidden locations
  [OK] No obfuscated or encoded strings
  [OK] No dynamic code loading (importlib, __import__)
  [OK] No credential handling or storage

================================================================
  CONCLUSION
================================================================

  The application is safe for use with sensitive research data.
  It operates entirely offline, uses no network connectivity,
  accesses no files outside its own input/output folders, and
  contains no security vulnerabilities.

  All 5 bandit findings are false positives related to XML
  parsing of the application's own generated output.

================================================================
